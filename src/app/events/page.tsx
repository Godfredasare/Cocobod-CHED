import EventsPageContent from './EventsPageContent';

export const metadata = {
  title: 'Events | CHED - Cocoa Health and Extension Division',
  description: 'Stay updated with CHED events, workshops, training sessions, and celebrations across Ghana.',
};

export default function EventsPage() {
  return <EventsPageContent />;
}
